package ru.taskhero.common.model.enums;

/**
 * Единица измерения для повторяющихся заданий.
 */
public enum RepeatUnit {
    /** Повторять каждые N дней. */
    DAYS,
    /** Повторять каждые N недель. */
    WEEKS,
    /** Повторять каждые N месяцев. */
    MONTHS
}
